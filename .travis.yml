os: linux
dist: focal

services:
    - docker

jobs:
  include: 
    - language: shell
      os: linux
      dist: focal
      script:
        - docker pull ubuntu:23.04
        - docker images
    - language: shell
      arch: ppc64le   
      dist: focal
      group: edge
      script:
        - docker run quay.io/podman/stable echo "Hello, world!"
        - docker images

        
